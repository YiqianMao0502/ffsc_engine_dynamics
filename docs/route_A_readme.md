# Route A（论文边界内）
状态：仅实现论文给出的公式与结构；缺失外部数值，暂不可数值闭合。

## 2.2.1 两相（MBWR-32）
- 已实现：式(2.1) p(ρ,T) 与表8的 α1..α15(T) 结构映射。
- 缺失：ρ_crit, b1..b32（每种介质一套）；u,h,s,cv,cp,A,G 的具体公式/实现。

## 2.2.2 气相（SRK/PR + 混合）
- 已实现：式(2.2) 的 SRK/PR P(T,v)；式(2.11) 混合定律；式(2.12)–(2.13) 变量关系。
- 缺失：各纯组分 Tc, Pc, ω, M；混合组成 Xi；(2.3)–(2.5) 的 a1..a9 与残余项；(2.6),(2.7) 输运系数。

使用方式：等待你提供相应数值表后，直接填入 JSON，再运行现有加载与计算入口。
